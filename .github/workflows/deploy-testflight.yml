name: Deploy to TestFlight

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    name: Deprecated EAS Build & Submit
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "The canonical BRDG TestFlight path is the local Xcode release flow."
          echo "Use npm run release:ios:prepare and npm run release:ios:ship from a clean main checkout."
